import React from "react";

const LandingPage = () => {
  const { refs } = useOutletContext();
  const {
    heroRef,
    gapRef,
    solutionsRef,
    howItWorksRef,
    partnersRef,
    tractionRef,
    ctaRef,
  } = refs;

  return (
    <section>
      <div></div>
    </section>
  );
};

export default LandingPage;
